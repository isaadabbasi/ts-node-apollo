version: "3.7"

services:
  postgres:
    image: postgres:12.3-alpine
    restart: always
    environment:
      - POSTGRES_USER=nft
      - POSTGRES_PASSWORD=password
    ports:
      - 5432:5432
    volumes:
      - postgres-data:/var/lib/postgresql/data/

volumes:
  postgres-data:
